---
apiVersion: v1
kind: Service
metadata:
  name: ecommerce-app
  namespace: apps
  labels:
    app: ecommerce-app
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 3000
    targetPort: 3000
    protocol: TCP
  selector:
    app: ecommerce-app
