---
# Example sealed secret - replace with your actual sealed secrets
apiVersion: v1
kind: Secret
metadata:
  name: personal-vault-secrets
  namespace: apps
type: Opaque
stringData:
  # Replace these with actual values or use sealed-secrets
  VAULT_TOKEN: "your-vault-token-here"
  ENCRYPTION_KEY: "your-encryption-key-here"
  DATABASE_URL: "your-database-url-here"
